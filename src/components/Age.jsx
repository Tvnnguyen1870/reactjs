const Age = (props) => {
    return (
        <div>
            <div> đây là tuổi : {props.age}</div>
            <div> đây là tên : {props.displayName}</div>
        </div>
    )
}
export default Age;